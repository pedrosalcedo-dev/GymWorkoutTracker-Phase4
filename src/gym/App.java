package gym;

/**
 * Pedro Salcedo
 * CEN 3024 - Phase 4
 *
 * launches the MainGUI so the
 * program starts with the interface.
 */
public class App {

    /**
     * Starts the program by opening the GUI.
     *
     * @param args command-line arguments (not used)
     */
    public static void main(String[] args) {
        MainGUI.main(args);
    }
}
