# Gym Workout Tracker – Phase 4 (SQLite Version)

This is my Phase 4 version of the Gym Workout Tracker for CEN 3024.  
For this phase, I updated my program to use a SQLite database instead of a text file.  
The user can load a `.db` file, view all workouts, and use the GUI to add, update, delete, 
and run my custom action (1RM + total volume).

## How to Run
1. Open the project in IntelliJ.
2. Make sure the SQLite JDBC driver is added (`lib/sqlite-jdbc-3.51.0.0.jar`).
3. Run `App.java`.
4. In the GUI, type the path to the database file:
